<Page x:Class="Movies.View.Admin.FilmsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Movies.View.Admin"
      mc:Ignorable="d" 
      d:DesignHeight="377" d:DesignWidth="781"
      Title="FilmsPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="329*"/>
            <RowDefinition Height="48*"/>
        </Grid.RowDefinitions>
        <DataGrid x:Name="FilmsGrid" AutoGenerateColumns="False" HorizontalAlignment="Left" Height="381" Margin="-8,-3,0,-1" VerticalAlignment="Top" Width="788" Background="#FFF3F3F3" Grid.RowSpan="2">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Название фильма" Width="150" Binding="{Binding Name}"/>
                <DataGridTextColumn Header="Режиссер" Width="150" Binding="{Binding producer_fio}"/>
                <DataGridTextColumn Header="Жанр" Width="200" Binding="{Binding Genre}"/>
                <DataGridTextColumn Header="Дата" Width="200" Binding="{Binding date}"/>
                <DataGridTextColumn Header="Страна" Width="200" Binding="{Binding Country}"/>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</Page>
